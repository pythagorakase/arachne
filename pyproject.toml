[project]
name = "arachne-loom"
version = "0.1.0"
description = "Agent-human decision loom and shared MCP adapter"
requires-python = ">=3.12"
dependencies = [
    "mcp==1.28.1",
]

[tool.uv]
package = false
